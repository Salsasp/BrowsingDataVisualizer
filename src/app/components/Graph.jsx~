import { Graph } from '@visx/network'


function Node(x, y) {



}
export default function BrowsingGraph(graph, width = 400, height = 300) {


  
  return (
    <svg width={width} height={height}>
      <rect width={width} height={height} rx={10} fill={'#cccccc'} />

      <Graph
	graph={graph}

      />
      
    
    </svg>
  )
}
